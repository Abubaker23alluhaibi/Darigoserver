# ============================================
# ⚠️ أمان حرج: هذا ملف مثال - لا ترفعه للـ Git!
# ============================================
# انسخ هذا الملف إلى .env واملأ القيم الحقيقية
# تأكد من أن .env موجود في .gitignore

# ============================================
# Database Configuration
# ============================================
# رابط قاعدة البيانات MongoDB Atlas
# ⚠️ مهم جداً: استبدل YOUR_USERNAME و YOUR_PASSWORD و YOUR_CLUSTER ببياناتك الحقيقية
# لا تستخدم بيانات حقيقية في هذا الملف - فقط في ملف .env الخاص بك
MONGODB_URI=mongodb+srv://YOUR_USERNAME:YOUR_PASSWORD@YOUR_CLUSTER.mongodb.net/darigo-real-estate?retryWrites=true&w=majority

# أو استخدم المتغيرات المنفصلة (أكثر أماناً):
# ATLAS_CLUSTER=your-cluster.mongodb.net
# ATLAS_USERNAME=your-username
# ATLAS_PASSWORD=your-password
# ATLAS_DATABASE=darigo-real-estate
# CONNECTION_TYPE=atlas

# ============================================
# Server Configuration
# ============================================
PORT=3001
NODE_ENV=production

# ============================================
# JWT Configuration
# ============================================
# ⚠️ حرج: يجب استخدام مفتاح قوي وعشوائي في الإنتاج
# استخدم أحد الأوامر التالية لتوليد مفتاح قوي:
# Linux/Mac: openssl rand -base64 32
# Windows: [Convert]::ToBase64String((1..32 | ForEach-Object { Get-Random -Minimum 0 -Maximum 256 }))
JWT_SECRET=your-super-secret-key-change-this-in-production-MUST-BE-STRONG-AND-RANDOM
JWT_EXPIRES_IN=7d

# JWT Refresh Secret (مفتاح منفصل للتجديد)
JWT_REFRESH_SECRET=your-refresh-secret-key-change-this-in-production-MUST-BE-STRONG-AND-RANDOM
JWT_REFRESH_EXPIRES_IN=30d

# ============================================
# Encryption Configuration
# ============================================
# مفتاح التشفير - يجب أن يكون 32 حرف على الأقل
ENCRYPTION_KEY=your-encryption-key-must-be-at-least-32-characters-long

# عدد جولات bcrypt (10-15 مناسب للإنتاج)
BCRYPT_ROUNDS=12

# ============================================
# Session Configuration
# ============================================
# مفتاح الجلسة - يجب أن يكون قوياً وعشوائياً
SESSION_SECRET=your-session-secret-key-change-this-in-production-MUST-BE-STRONG

# ============================================
# CORS Configuration
# ============================================
# يمكنك إضافة عدة أصول مفصولة بفاصلة
# مثال: CORS_ORIGIN=https://yourdomain.com,https://www.yourdomain.com
# ملاحظة: Railway domains مسموحة تلقائياً (*.railway.app)
# في الإنتاج، يجب تعيين هذا المتغير
CORS_ORIGIN=https://web-production-2d009.up.railway.app

# ============================================
# Security Settings
# ============================================
COOKIE_SECURE=true
COOKIE_HTTP_ONLY=true

# ============================================
# Optional: Additional Settings
# ============================================
# UPLOAD_MAX_SIZE=10485760
# ALLOWED_FILE_TYPES=jpg,jpeg,png,gif,pdf
# LOG_LEVEL=info
